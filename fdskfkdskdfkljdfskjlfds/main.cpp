#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define fast ios_base::sync_with_stdio(0),cin.tie(0),cout.tie(0);
int matr[8][8];
string s,s1;
int x,x1,y,y2;
signed main(){
    cin>>s>>s1;
    x=int(s[0]-'a')+1;
    x1=int(s1[0]-'a')+1;
    y=int(s[1]-'1')+1;
    y2=int(s1[1]-'1')+1;
    if((abs(x-x1)==2 && abs(y-y1)==1) || (abs))
    return 0;
}
